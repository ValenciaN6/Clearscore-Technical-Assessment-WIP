# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'clearscoreProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for clearscoreProject
   
  target 'clearscoreProjectTests' do
    inherit! :search_paths
    pod ‘Cuckoo’
  end

  target 'clearscoreProjectUITests' do
    # Pods for testing
  end

end
